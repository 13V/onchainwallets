# Query results and generated query IDs — may contain an account's private data.
out/

# Never commit an API key.
.env
*.key

# Local run logs
*.log

# Results are committed under results/, not out/
!results/
